import React from 'react';

const Filters = ({ onChange }) => {
    // Implement filter options and handle change
    return (
        <div>
            {/* Implement filter options */}
        </div>
    );
};

export default Filters;
